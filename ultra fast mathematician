#include<stdio.h>
#include<string.h>

int main()
{
	char a[1000],b[1000],c[1000];
	scanf("%s %s",a,b);
	size_t length =strlen(a);
	for(int i=0;i<length;i++)
	{
		if(a[i]==b[i])
		{
			c[i]='0';
			}
			else
			{
				c[i]='1';
				}
		}
		c[length]='\0';
		printf("%s",c);
	}