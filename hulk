#include<stdio.h>
int main()
{
	long long int n;
	scanf("%lld",&n);
	for(long long int i=1;i<=n;i++)
	{
		if(i%2==1)
		{
			printf("I hate");
			}
			else
			{
				printf("I love");
				}
				if(i!=n)
				printf(" that ");
			
		}
		printf(" it\n");
		return 0;
	}