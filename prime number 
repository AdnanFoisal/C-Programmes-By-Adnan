#include<stdio.h>
  #include<math.h>
  int main()
  {
  	int num,u,i;
  scanf("%d",&num);
  if(num<2)
  {
  	printf("Not Prime");
  return 0;
  	}
  if(num>2 && num%2==0)
  {
  printf("Not Prime");
  return 0;
  }

  for(i=3;i*i<=num;i+=2)
  {
  	if(num%i==0)
  {
  printf("Not Prime");
  return 0;
  }
  	}
  
  printf("Prime");
  return 0;
  	}